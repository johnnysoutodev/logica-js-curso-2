name: Sync Develop to Deploy

permissions:
  contents: write
  actions: write

on:
  push:
    branches: [develop]

jobs:
  sync-to-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.PAT_TOKEN }}  # ✅ Mudança aqui!

      - name: 🔄 Configure Git
        run: |
          git config user.name github-actions
          git config user.email github-actions@github.com

      - name: 🌿 Create and sync deploy branch
        run: |
          # Busca todas as branches remotas
          git fetch origin
          
          # Verifica se a branch deploy já existe remotamente
          if git ls-remote --heads origin deploy | grep -q deploy; then
            echo "Branch deploy existe, fazendo checkout..."
            git checkout -B deploy origin/deploy
            git merge origin/develop --no-ff -m "chore: sync develop to deploy [trigger-deploy]"
          else
            echo "Branch deploy não existe, criando..."
            git checkout -b deploy origin/develop
          fi
          
          # Push da branch deploy (isso irá trigger o deploy.yml)
          git push origin deploy
      
      - name: 🔍 Verify deploy branch
        run: |
          echo "Branch deploy criada/atualizada com sucesso!"
          echo "Deploy workflow será acionado automaticamente..."
          git branch -a